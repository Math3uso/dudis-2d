#pragma once

#include "raylib.h"

namespace dudis {

class Shape {
protected:
  // Color color = WHITE;

public:
  Shape() {};
  // void setColor(Color nColor) { color = nColor; }
  // Color getColor() const { return color; }
};
} // namespace dudis